from map import Map
from world import World
from player import Player
from config import Config


if __name__ == "__main__":
    gameConfig = Config()
    world = World()
    myMap = Map()
    myPlayer = Player()
